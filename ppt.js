let myString = "hello"; // 문자열을 myString에 할당
myString.toUpperCase(); // 대문자로 변환
console.log(myString); // "hello"가 출력


const number = 10;
const number2 = 3.14;
const number3 = -5;

const string = "Hello";
const string = "123";

const t = true;
const f = false;

console.log(t === f); // false

const n = null;

let und = undefined;

let id = Symbol();
// 심볼 id에는 "id"라는 설명이 붙습니다.
let id = Symbol("id");

let id1 = Symbol("id");
let id2 = Symbol("id");

alert(id1 == id2); // false


var arr = ['수지', '도하', '두희']

function fnc(x){ return 0;}


 new Date();

new RegExp(/ab+c/,'i')
new RegExp( 'ab+c' , 'i' )

const array1 = [1, 4, 9, 16];


arr.map(function(element, index, array){  }, this);